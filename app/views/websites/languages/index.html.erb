<% content_for :title, Website::Language.model_name.human(count: 2) %>

<%= link_to t('actions.create'), new_website_language_path, class: "btn btn-primary mb-3" %>

<% if @languages.any? %>
  <%= render 'list', languages: @languages %>
<% end %>
