<div class="card">
  <div class="card-body">
    <h2 class="card-title">
      <%= link_to group, website_group_path(group, { website_id: group.website_id }), class: 'text-decoration-none' %>
    </h2>
    <p><%= group.pages.size %> <%= Website::Page.model_name.human(count: 2).downcase %></p>
  </div>
</div>
